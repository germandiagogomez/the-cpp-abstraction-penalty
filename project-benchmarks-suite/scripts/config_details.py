#!/usr/bin/env python3
import platform
import os.path as op
from Pathlib import Path

from util import BUILD_DIR_BASE

config_folder = '!'.join(CONFIG_NAMES)
config_details_dir = 'config_details'

for config in CONFIG_NAMES:
    print(config_name)
    #with open(Path(config_details_dir) / )
